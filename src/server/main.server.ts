/**
 * Servidor 3D — constrói o mapa na inicialização. Zero Interface:
 * tudo é Part no Workspace.
 */
import { construirMapa } from "./mapa";

construirMapa();
print("[3D] Servidor no ar (mapa em código, sem UI).");
